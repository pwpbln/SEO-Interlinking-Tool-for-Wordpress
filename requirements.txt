# torch hat kein Binary für macOS Intel + Python 3.13.
# sentence-transformers ist durch fastembed ersetzt (ONNX Runtime, kein torch nötig).
# Installation: bash install.sh

# Web-Scraping
requests==2.32.3
beautifulsoup4==4.12.3
markdownify==0.13.1
lxml==5.3.0

# Hilfspakete
python-dotenv==1.0.1

# Numerik – vor fastembed installieren (install.sh erzwingt die Reihenfolge)
numpy==2.2.1

# Embeddings via ONNX Runtime (kein torch, läuft auf macOS Intel + Python 3.13)
fastembed>=0.5.0

# Clustering und Datei-Überwachung
scikit-learn>=1.3.0
watchdog>=4.0.0
